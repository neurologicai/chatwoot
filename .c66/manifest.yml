postgresql:
  server: external

# Configurações do Rails para desabilitar compilação automática
rails:
  configuration:
    asset_pipeline_enabled: false
    asset_pipeline_precompile: false

# Configurações do Node.js
node:
  version: 23
